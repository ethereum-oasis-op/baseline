pragma solidity ^0.8.13;

contract Verifier {
	function verifyProof(
		uint256[] calldata _proof,
        	uint256[] calldata _publicInput
	) external returns (bool result){
	
	//call the zkp verifier function
        return true;
	}
}