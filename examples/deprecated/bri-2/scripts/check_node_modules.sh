#!/bin/bash

echo "Checking for ./node_modules/"
DIRECTORY="./node_modules"
if [ -d $DIRECTORY ]; then
    echo "  Directory '$DIRECTORY' exists. No action required."
else 
    echo "  Directory '$DIRECTORY' does not exist. Running 'npm install' now"
    npm install
fi
